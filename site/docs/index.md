---
template: home.html
title: Deterministic testing for Spring AI
description: >-
  Spring AI Test Tools is a deterministic testing framework for Spring AI
  applications. Golden AI responses, zero-token replays, and test code that
  never changes.
hide:
  - navigation
  - toc
---

<!--
  The landing page is rendered entirely by `overrides/home.html`, which replaces
  Material's `container` block — so this file's body is intentionally not shown.
  Edit the landing markup in site/overrides/home.html and its styles in
  site/docs/stylesheets/landing.css.

  The front matter above still matters: `template` selects the custom layout,
  `title`/`description` feed the <title> tag and the meta description, and `hide`
  keeps the nav sidebar and table of contents off the landing.

  Everything this page presents has a prose counterpart in README.md (same first
  sentence, the same eight capabilities, the same measured benchmark numbers, the
  same comparison table, the same package tree) — keep the two in step when
  either changes.
-->
